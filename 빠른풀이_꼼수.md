# 계산 문제 빠른풀이 꼼수 모음

**목적은 정답을 아는 것이 아니라 "계산을 줄이는 것"이다.** 여기 있는 요령은 전부
`Theory_Summary.md`·`Machinery_Summary.md`·`Facility_Summary.md` 의 공식과 같은 것이고,
**시험에서 4지선다 보기의 생김새를 이용해 계산을 생략하는 법**만 따로 모았다.
공식 자체를 모르면 이 요령도 소용없다 — 공식은 저 세 문서에서 외운다.

> 여기 나온 숫자는 전부 계산해서 확인한 것이다(오늘 날짜 기준). 이해 안 되는 항목은
> `오답_체크리스트.md` 에 적어 알려줄 것 — 실제로 틀리는 문제 패턴을 보고 더 추가한다.

---

## 1. 보기의 지수(10의 몇 제곱)가 다 같으면 지수는 아예 안 본다

전기이론 계산 문제 상당수가 **"공식 상수 × 숫자들"** 꼴이라, 출제자가 오답을 만들 때
**지수는 그대로 두고 앞자리 숫자만 바꾸는 경우**가 많다. 이때는 지수 계산을 통째로
생략하고 **앞자리 숫자끼리만 곱하고 나누면** 그게 정답의 앞자리다.

**적용되는 공식** (전부 "상수 × 숫자들의 곱/나눗셈" 꼴이라 같은 요령이 통한다):
$$F = 9\times10^9 \cdot \frac{Q_1 Q_2}{r^2}, \quad E = 9\times10^9 \cdot \frac{Q}{r^2}, \quad V = 9\times10^9 \cdot \frac{Q}{r}, \quad F_{자기} = 6.33\times10^4 \cdot \frac{m_1 m_2}{r^2}$$

> **실제 기출 예제** — "공기 중에 놓여 있는 $2\times10^{-7}$ C 의 전하로부터 10 cm 거리에
> 있는 점의 전장의 세기는?" 보기가 **① $1.2\times10^5$ ② $1.8\times10^5$ ③ $3.6\times10^5$
> ④ $4.2\times10^5$** 처럼 **지수가 전부 $10^5$ 로 같다.** 이럴 때 지수는 안 본다.
>
> 앞자리만 계산: 공식 상수 **9**, 전하량 숫자 **2**, 거리는 10 cm = 0.1 m → **제곱해도 그대로 관여**.
> $9 \times 2 = 18$ → **1.8**. 지수는 $9 - 7 + 2 = 4$(전하 지수 $-7$, 거리 $0.1^2=10^{-2}$ 를
> 나누는 것이므로 $+2$) → $10^4$. 합치면 **$1.8\times10^5$** — 보기 **②**.
> (파이썬으로 실제 계산해도 179999.99...≈$1.8\times10^5$, 일치함.)

> **왜 되는가** — 보기 ①②③④ 의 지수 부분이 전부 $10^5$ 처럼 같다면, 그건 출제자가
> "지수는 계산하는 사람이 다 맞힐 거고, 진짜 헷갈리게 할 곳은 앞자리"라고 설계했다는
> 뜻이다. **보기부터 먼저 훑어서 지수가 다 같은지 확인**하는 습관을 들이면, 지수가 같을 땐
> 뒷자리(지수) 계산을 통째로 건너뛰고 앞자리 숫자만 암산해서 맞는 보기를 고르면 된다.
> **보기의 지수가 서로 다르면 이 요령은 못 쓴다** — 그때는 정공법으로 다 계산해야 한다.

---

## 2. 계산 자체가 필요 없는 통암기 상수

숫자 하나만 곱하거나, 아예 계산 없이 값 자체가 답인 것들. **여기 있는 건 계산이 아니라
암기 문제다** — 아래 값을 몰라서 유도하려고 하면 오히려 시간 손해다.

| 상황 | 그냥 곱하는 숫자 / 정해진 값 | 확인 |
| :--- | :--- | :---: |
| 단상 반파 정류 평균전압 | 교류전압 × **0.45** | 100V→45V ✓ |
| 단상 전파 정류 평균전압 | 교류전압 × **0.90** | 100V→90V ✓ |
| 3상 반파 정류 평균전압 | 교류전압 × **1.17** | 100V→117V ✓ |
| 3상 전파 정류 평균전압 | 교류전압 × **1.35** | 100V→135V ✓ |
| Y-Δ 기동 시 전류·토크 | 항상 델타 운전의 **1/3배** | $(1/\sqrt3)^2=1/3$ ✓ |
| V결선 출력비 | 원래 용량 합의 **57.7 %** | — |
| V결선 이용률 | **86.6 %** | $\sqrt3/2$ |
| 3상 전력·V결선 계산에 쓰는 $\sqrt3$ | **1.732** 로 바로 대입 | — |

> **왜 계산하면 손해인가** — 예를 들어 "100kVA 변압기 2대를 V결선하면 출력은?" 같은 문제는
> $\sqrt3 \times 100$ 을 매번 계산하는 대신 **"200의 86.6 % = 173.2"** 또는
> **"100의 173.2 %"** 를 바로 떠올리면 끝난다. 시험에 쓰는 상수는 몇 개 없으니
> 이 표를 통째로 외우는 게 매번 유도하는 것보다 빠르다.

---

## 3. 값의 범위·성질로 말이 안 되는 보기부터 지운다

계산해서 정답을 맞히는 게 아니라 **계산 없이 오답부터 지우는** 방법이다.
4지선다에서 오답 하나만 지워도 확률이 올라간다.

* **극수(P)는 항상 짝수다.** 자석은 N·S 쌍으로만 존재하므로 2, 4, 6, 8, ... 만 나온다.
  보기에 3극, 5극처럼 **홀수 극수가 있으면 그건 무조건 오답**이다.
* **병렬 합성저항은 항상 가장 작은 개별 저항보다 작다.**
  (2Ω, 3Ω 병렬 → 1.2Ω. 2Ω보다 큰 보기는 계산 안 해도 오답.)
* **직렬 합성 정전용량도 항상 가장 작은 개별 용량보다 작다** — 저항 병렬과 똑같은 식이라
  똑같은 성질을 가진다. (2μF, 3μF 직렬 → 1.2μF)
* **유도전동기 슬립은 보통 2.5~10 % 사이다** (소형 5~10 %, 대형 2.5~5 %). 계산 결과가
  30 %, 50 % 처럼 크게 나오면 식을 잘못 세운 것이다 — **답이 아니라 검산 신호**로 쓴다.
* **전기설비 숫자는 딱 떨어지게 낸다.** 지지점 간격·절연저항·전압강하 계산에서
  소수점 넷째 자리까지 지저분하게 나오면 대개 계산 실수다 — 문제를 다시 읽는다.

---

## 4. 거리 배수는 암산으로 — 제곱인지 그냥 반비례인지만 구분

전기이론에서 "거리가 n배가 되면 얼마가 되는가"를 물으면 **계산 없이 배수만 알면 끝난다.**
헷갈리는 건 딱 하나 — **제곱에 반비례**인지 **그냥 반비례**인지다.

| 무엇 | 거리와의 관계 | 거리 2배면 | 거리 3배면 |
| :--- | :--- | :---: | :---: |
| 쿨롱의 힘 $F$ | $\propto 1/r^2$ | **1/4** | 1/9 |
| 전계 $E$ | $\propto 1/r^2$ | **1/4** | 1/9 |
| 자기 쿨롱의 힘 | $\propto 1/r^2$ | **1/4** | 1/9 |
| 전위 $V$ | $\propto 1/r$ | **1/2** | 1/3 |
| 무한 평면의 전계 | 거리 무관 | **그대로** | 그대로 |

> **암기 요령** — "힘·전계"(둘 다 벡터, 방향이 있는 것)는 **제곱 반비례**,
> "전위"(방향 없는 스칼라, 에너지 개념)는 **그냥 반비례**. 무한 평면은 아예 안 변한다.
> 이 표 하나면 "거리를 몇 배로 하면?" 유형은 계산 없이 암산으로 끝난다.

---

## 5. 검산용 감 — 정수로 안 떨어지면 의심한다

기능사 시험은 **깔끔한 숫자가 나오게 설계**하는 경우가 많다. 계산했는데 숫자가
지저분하면(예: 13.7183...) 식을 잘못 세웠을 확률이 높다.

* **변압기 권수비**: 1차/2차 전압을 나누면 대개 정수(15, 20, 30배처럼)로 떨어진다.
  (3300V/220V=15, 3300V/110V=30 — 둘 다 정수)
* **동기속도**: $120f/P$ 는 $P$ 가 짝수인 이상 60Hz·50Hz 어느 쪽이든 항상 정수 rpm이 나온다.
  소수점이 나오면 극수나 주파수를 잘못 읽은 것이다.
* **정류 평균전압**: 0.45/0.90/1.17/1.35 를 곱한 결과가 딱 떨어지지 않아도 되지만
  (전압 자체가 홀수일 수 있으므로), **배수 자체(0.45 등)를 잘못 골랐는지는 항상 재확인**한다
  — 단상↔3상, 반파↔전파를 바꿔 읽는 게 제일 흔한 실수다.

---

## 6. 분배·발열은 나눗셈 안 해도 "크고 작음"만으로 답이 나온다

"어느 쪽 전압이 더 큰가", "어느 저항이 더 뜨거운가" 유형은 **비율 계산 없이 크기 비교만
하면 된다.** 값을 실제로 나누는 건 숫자로 답해야 할 때뿐이다.

| 상황 | 규칙 | 확인 |
| :--- | :--- | :---: |
| 저항 **직렬** 전압분배 | **저항이 큰 쪽에 전압도 크다** ($V\propto R$) | $R{=}2,8\Omega$ → $V{=}2,8$V |
| 저항 **병렬** 전류분배 | **저항이 작은 쪽에 전류가 크다** ($I\propto 1/R$) | 같은 회로 → $I{=}8,2$A |
| 콘덴서 **직렬** 전압분배 | **용량이 작은 쪽에 전압이 크다** (저항과 반대!) | — |
| 저항 **직렬** 발열 | 전류가 같으므로 $I^2R$ → **저항 큰 쪽이 더 뜨겁다** | — |
| 저항 **병렬** 발열 | 전압이 같으므로 $V^2/R$ → **저항 작은 쪽이 더 뜨겁다** | — |

> **하나로 꿰기** — 직렬은 전류가 공통이라 저항이 클수록 전압도 크고 열도 더 난다.
> 병렬은 전압이 공통이라 저항이 작을수록 전류도 크고 열도 더 난다. **"직렬=저항 클수록 세짐,
> 병렬=저항 작을수록 세짐"** 한 문장으로 외우면 전압·전류·발열 세 가지 질문에 다 쓴다.
> 콘덴서는 저항과 정확히 반대(직렬에서 작은 용량이 큰 전압)라는 것만 따로 기억한다.

---

## 7. RLC 위상 — 각도 계산 없이 크기만 비교한다

$X_L$ 과 $X_C$ 를 계산했으면, $\tan\theta$ 나 위상각까지 구할 필요 없이 **둘 중 어느 게
큰지만 보면** 유도성인지 용량성인지 바로 나온다.

* $X_L > X_C$ → **유도성** (코일이 이긴다, 전류가 전압보다 **뒤진다/지상**)
* $X_L < X_C$ → **용량성** (콘덴서가 이긴다, 전류가 전압보다 **앞선다/진상**)
* $X_L = X_C$ → **공진** (임피던스가 저항 $R$ 만 남는 최솟값, 전류 최대)

> **예제** — $f{=}60$Hz, $L{=}0.1$H, $C{=}100\mu$F 이면 $X_L{\approx}37.7\Omega$,
> $X_C{\approx}26.5\Omega$. $X_L>X_C$ 이므로 **유도성, 지상** — 각도를 몰라도 "지상이냐 진상이냐"
> 묻는 문제는 이걸로 끝난다.

---

## 8. 극단값을 대입하면 공식을 안 써도 되는 경우

슬립처럼 **범위의 양 끝 값(0 또는 1)에서 물어보는 문제**는 공식을 쓰지 않고 상식으로 바로 답한다.

* **슬립 $s=1$**: 기동 순간(정지 상태). $N=(1-s)N_s = 0$ — 안 돌고 있다는 뜻이니 당연하다.
* **슬립 $s=0$**: 동기속도로 돌 때(이상적인 무부하). $N=N_s$ — 회전자와 자계가 같은 속도라
  더 이상 유도전류가 생기지 않는다(그래서 토크도 0).
* **2차 효율 $\eta_2 = 1-s = N/N_s$**: 슬립만 알면 뺄셈 한 번으로 끝난다. 슬립 5%면
  효율은 계산할 것도 없이 **95%**.

---

## 9. "깔끔한 조합"으로 나오게 설계된 계산들

기능사 계산 문제는 **피타고라스 3-4-5, 제곱해서 정수 되는 값**처럼 손으로 풀기 좋은
조합으로 낸다. 숫자를 보고 그 조합인지 먼저 의심하면 계산이 훨씬 짧아진다.

* **변압기 전압변동률 최댓값** $\varepsilon_{max}=\sqrt{p^2+q^2}$ — $p{=}3,\,q{=}4$ 처럼
  나오면 답은 굳이 계산 안 해도 **5**(3-4-5 직각삼각형)다.
* **변압기 최적 부하율** $m=\sqrt{P_i/P_c}$ — $P_i{:}P_c$ 가 $1{:}4$, $1{:}9$ 처럼 제곱비로
  나오면 $m$ 은 각각 **0.5, 1/3**처럼 깔끔하게 떨어진다. 루트 안이 딱 떨어지는 분수가
  아니면 문제를 잘못 읽었을 확률이 높다.
* **R-L-C 임피던스** $Z=\sqrt{R^2+(X_L-X_C)^2}$ — $R,\,X$ 가 **3-4-5 나 6-8-10 의 배수**로
  나오면(예: $R{=}8,\,X{=}6$) 제곱해서 더할 것도 없이 $Z$ 는 나머지 한 변인 **10**이다.
  기능사 계산 문제의 8~9할이 이 비율로 나온다.
* **동기속도** $N_s=120f/P$ — 60 Hz 는 $120\times60=\mathbf{7200}$ 을 미리 계산해두고
  **극수로 나누기만** 하면 된다($7200/4=1800$). 매번 $120\times60$ 을 다시 곱할 필요 없다.

---

## 10. 전기설비 — 계산이 아니라 숫자 하나로 갈리는 것들

전기설비는 원래 계산이 적은 과목이지만, 있는 계산도 **상수 하나만 정확히 골라내면
끝나는 대입형**이다. 유도 없이 아래만 확인한다.

* **전압강하 공식의 상수 3개는 배선방식이 답을 정해준다** — 계산이 아니라 **어느 상수를
  쓸지 고르는 문제**다.
  | 배전방식 | 곱하는 상수 |
  | :--- | :---: |
  | 단상 2선식 | **35.6** |
  | 3상 3선식 | **30.8** |
  | 단상 3선식 · 3상 4선식 | **17.8** |
  → 배선방식만 읽고 상수를 고른 뒤 $L\cdot I / (1000A)$ 만 계산하면 끝.
* **금속관 굵기는 숫자의 홀짝으로 후강/박강이 갈린다** — 16·22·28... **짝수는 후강**
  (안지름 기준), 19·25·31... **홀수는 박강**(바깥지름 기준). 계산할 게 없고 숫자만 보면 된다.
* **절연저항은 두 값 중 하나** — SELV·PELV 만 **0.5 MΩ**, 나머지는 전부 **1.0 MΩ**.
  전로 종류를 확인하는 순간 답이 정해진다.
* **누전차단기도 두 값 중 하나** — 물기 있는 곳 콘센트만 **15 mA**, 나머지는 **30 mA**
  (동작시간은 둘 다 0.03초로 같다).

---

## 다음에 추가할 것

문제 풀다가 "이것도 계산 없이 되네" 싶은 게 나오면 그 자리에서 여기 추가한다.
`오답_체크리스트.md` 에 적은 것 중 **"계산 실수로 틀린 것"** 은 특히 이 문서로 옮겨질
후보다 — 왜 틀렸는지가 "몰라서"가 아니라 "굳이 다 계산해서"였다면, 여기에 요령이
빠져 있다는 뜻이다.
